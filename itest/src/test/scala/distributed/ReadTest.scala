package distributed

/**
  * @Author: Airzihao
  * @Description:
  * @Date: Created at 13:17 2019/11/30
  * @Modified By:
  */
class ReadTest {

  /*
  extends the Drivertest
  1. add a cypher func (or key-word) to get the real server-address. (need )
    (or any other method to test whether the read-request be reposted to different server.)

  test the follwing style-like cypher statement, to make sure all methods are usable
  1. Match (n:Label) where n.$prop1 = $value1 and n.$prop2 = $value2
  2. Match (n) where id(n) = $value ...
  3. other possible cypher syntax
   */
}

